#include <stdio.h>

int x, y;

int main() {
    printf("Digite o valor de x: ");
    scanf("%d", &x);
    printf("Digite o valor de y: ");
    scanf("%d", &y);

    printf("SOMA = %d", x + y);
}
